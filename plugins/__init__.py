# Plugin registry — this directory contains embedded third-party repos.
# Each subdirectory is a self-contained project managed by its own package
# manager (uv) and exposed to the parent pipeline through MCP.
